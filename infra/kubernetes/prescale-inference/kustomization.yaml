# Kustomization for Prescale Inference Service
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prescale

resources:
  - namespace.yaml
  - configmap.yaml
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - pod-monitoring.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/part-of: prescale
  app.kubernetes.io/managed-by: kustomize

# Image configuration (override in overlays)
images:
  - name: prescale-inference
    newTag: "0.1.0"
